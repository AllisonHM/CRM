# =============================================
# 🔐 VARIÁVEIS DE AMBIENTE
# =============================================
#
# Criar este arquivo na raiz do projeto: .env
# NÃO COMITAR ESTE ARQUIVO NO GIT!
# Adicionar .env ao .gitignore
#
# =============================================

# API Key para site institucional (gerar chave forte)
# Exemplo de geração: python -c "import secrets; print(secrets.token_urlsafe(32))"
API_KEY_SITE=sua-chave-secreta-aqui-123456

# Origens permitidas (separadas por vírgula)
# Em desenvolvimento:
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5000,http://127.0.0.1:5000

# Em produção (adicionar):
# ALLOWED_ORIGINS=https://seu-site.github.io,https://seu-site.netlify.app

# =============================================
# INTEGRAÇÃO META (FACEBOOK / INSTAGRAM)
# =============================================
# Obtenha em: https://developers.facebook.com/apps/
# META_APP_ID     → ID numérico do App Meta
# META_APP_SECRET → Chave secreta (nunca exponha ao cliente)
# META_REDIRECT_URI → URL exata cadastrada como "Valid OAuth Redirect URI" no App
META_APP_ID=seu_meta_app_id_aqui
META_APP_SECRET=seu_meta_app_secret_aqui
META_REDIRECT_URI=http://localhost:5000/auth/meta/callback

# ID do usuário CRM que receberá os leads do site
# Normalmente o ID do admin/super_admin (1 por padrão)
USUARIO_CRM_LEADS=1

# =============================================
# 📧 EMAIL (RECUPERAÇÃO DE SENHA)
# =============================================
# Gmail (recomendado com senha de app)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=true
MAIL_USE_SSL=false
MAIL_USERNAME=teujeitinho@gmail.com
MAIL_PASSWORD=tibf npjf jcgq czgt
MAIL_DEFAULT_SENDER=teujeitinho@gmail.com
